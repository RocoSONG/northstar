# 增长罗盘 · Northstar — 产品需求文档（PRD v0.4）

> 面向电商运营者的 AI 用户增长 Copilot：输入项目资源、背景、指标 → 自动生成企业级用增方案 → 上线后接入数据 → 实时追踪、复盘、预警。

---

## 变更记录

| 版本 | 修订内容 |
|---|---|
| v0.2 | 初版：三要素输入、方案生成、数据接入、动态看板、预警复盘 |
| v0.3 | ① 实时追踪改为 mock 数据流做实（保留"实时"定位，不降级）；② 三要素保持手动填写 + 增加预设；③ 字段映射改「规则优先 + LLM 兜底」；④ 预估模型标注简化版；⑤ 指标 Schema 补全；⑥ 补数据安全与 API key 安全；⑦ 看板冷启动预填充；⑧ 预警少而准；⑨ 复盘报告加价值密度标准 |
| v0.4 | ⑩ 去业务化：玩法/利益点/资源位改为由用户业务背景动态生成，不预设特定行业打法（业务无关原则） |

---

## 1. 产品概述

**一句话定位**：把一次用户增长活动，从「拍脑袋写方案 + 手工盯数据」，变成「输入三个要素，AI 生成可落地方案；上线后数据流进来，AI 帮你盯、帮你复盘、帮你预警」。

**核心价值**：把资深运营的「方案设计能力 + 券规则经验 + 数据敏感度」沉淀为可复用的 AI 助手。

**与市面差异**：
- 不是通用 AI 写作（不懂券规则约束、不会给数据预估）。
- 不是 BI 看板（只会展示数据，不会生成方案）。
- 是「**方案生成 → 数据追踪 → 复盘预警**」打通的垂直 Copilot。

**核心设计原则 · 业务无关**：产品不预设任何特定行业/公司的业务打法。玩法、利益点、选品匹配等「业务相关」的创意，全部由 LLM 根据用户输入的业务背景（行业、平台、资源、目标）动态生成；产品只提供「业务无关」的确定性底座——券合规校验、预估模型、异常检测（均为通用商业逻辑）。产品懂的是「增长的方法」，而不是「某个公司的打法」。

## 2. 目标用户

- **主**：电商平台 / 品牌方的用户增长、活动运营、品类运营（京东、得物、抖音电商、字节等）。
- **次**：品牌方电商团队（三顿半等新消费品牌）。

## 3. 核心闭环（产品灵魂）

```text
① 策划(生成方案) → ② 上线(数据接入) → ③ 复盘(追踪+预警) → 反哺下次策划
```

复盘模块产出的数据，就是下一次策划的输入（"上次去库存活动滞销 3 个 SKU，这次选品规避"）。

**反哺机制**：每次复盘后，方案的实际指标数据自动沉淀为「指标基线库」（按「平台 + 目标类型」归档），成为下次同类方案的 `baseline` 与预估模型的输入。

---

## 4. 操作全链路（用户旅程）

### Step 1 · 新建项目（手动填写 + 预设辅助）
三要素手动填写，但用**预设大幅降低填写成本**：

- **项目背景**：名称、平台（预设下拉：京东/得物/抖音电商/小红书）、目标（预设：拉新/促活/去库存/大促）、时间节点、目标人群。
- **项目资源**：预算、渠道（预设多选）、货盘（SKU 池）、券/促销工具（预设勾选）。
- **项目指标**：从「指标模板」按「平台 + 目标」一键加载默认集，可增删改。

> 交互上：先选「平台 + 目标」→ 系统自动带出对应的默认资源字段与指标集 → 用户只需微调，不用从零填三张表单。

### Step 2 · 生成方案
点击「生成用增方案」→ AI 输出**结构化方案**（JSON，非散文）：

| 子项 | 说明 |
|---|---|
| 玩法设计 | 由用户业务背景动态生成（电商可用满减/折扣，会员制可用权益/积分），不预设固定打法 |
| 利益点 | 匹配活动目标的利益点组合，由业务背景 + 目标驱动 |
| 选品清单 | 从货盘匹配商品 + 匹配理由（选品是方案的**一个输出环节**） |
| 优惠券规则 | 满减阈值 / 叠加 / 成本，**带合规校验**（业务无关规则） |
| 资源位排期 | 按用户可用的触达渠道安排（由用户资源决定） |
| 预估 GMV | 基于历史数据的预估（MVP 简化版） |
| 风险提示 | 库存 / 资损 / 客诉风险 |
| **指标清单(metricSchema)** | 本次要追踪的指标、单位、目标值、基线、预警规则（见 §7） |

### Step 3 · 方案上线
确认方案 → 项目进入「追踪态」。

> **冷启动预填充**：上线后看板**立即用预估数据 / mock 数据预填充**，用户先看到"上线后长什么样"；数据状态标注为「预估」，真实数据流入后覆盖。避免看板空窗。

### Step 4 · 数据接入（见 §6）
上传数据文件（或手动录入 / API 接入）→ 字段智能映射 → 看板开始流转。

### Step 5 · 追踪 / 复盘 / 预警
看板实时展示指标 + 趋势；异常自动预警（少而准）；AI 生成复盘报告 + 行动建议。

---

## 5. 功能清单

| 模块 | 功能 | 说明 |
|---|---|---|
| 项目 | 新建 / 列表 / 状态（草稿-已上线-已复盘） | 多项目管理 |
| 策划 | 三要素输入（含预设）+ 方案生成 + 方案编辑 | 核心生成 |
| 券校验 | 规则引擎防资损 | 见 §8 |
| 数据接入 | 文件导入 / 手动录入 / API 接入（mock 实时） | 见 §6 |
| 看板 | 动态指标看板（指标卡片 + 图表 + 趋势） | 见 §7 |
| 预警 | 阈值 / 环比 / 连续异常预警（少而准） | 自动检测 |
| 复盘 | AI 复盘报告 + 行动建议 | LLM 生成 |
| 对话助手 | 自然语言引导（"帮我策划一个防晒季拉新活动"） | tool use |

---

## 6. 核心设计一：数据如何接入（操作不复杂）

### 设计原则
> **90% 用户走最省事的一条路（文件导入），一次配置反复用；实时数据流用 mock 做实体验，真实 API 架构预留。**

### 路径 A：文件导入（主推，最省事）
1. **拖拽上传** CSV / Excel。
2. 系统提供**标准数据模板**（按本次方案的指标 Schema 自动生成空模板，可下载填数）。
3. **字段智能映射（规则优先 + LLM 兜底）**：
   - 主力：内置**同义词字典 + 编辑距离匹配**（"销售额/GMV/成交额/交易额"→`gmv`），确定性、零成本、即时。
   - 兜底：规则匹配不了的列，才调 LLM 识别。
   - 默认**全自动匹配 + 仅让用户确认**（识别不准才手动下拉微调一次），非让用户参与映射。
4. 记住映射关系，**之后每次上传免重复配置**。

### 路径 B：手动快速录入
适合小数据量 / 临时补数，核心指标直接填。

### 路径 C：实时数据流（mock 做实，真实 API 预留）
- **V1 用「mock 数据流引擎」模拟实时推送**：方案上线后，看板数据按预设趋势**定时流入更新**，呈现"数据实时在动"的体感（体验真实，数据为模拟）。
- 架构上，mock 数据流引擎是一个**可替换的数据源适配器**，未来接真实 API / WebSocket 时替换适配器即可。
- **数据安全合规（诚实标注）**：demo 阶段全量 mock；真实接入企业数据需**数据脱敏 + 权限隔离 + 合规审查**，此为架构预留项，不在 MVP。

---

## 7. 核心设计二：数据看板如何动态承接不同方案（指标 Schema 驱动）

### 问题
每个项目的方案不同 → 追踪的指标、数据、预警都不同 → **看板是固定模板吗？怎么每次承接不同方案？**

### 答案：不是固定模板，是「指标 Schema 驱动」的动态看板

**机制（三层）：**

1. **方案生成时，同步输出一份 `metricSchema`（指标清单）**，字段完整到足以驱动渲染：
```json
{
  "metrics": [
    {
      "key": "gmv",
      "label": "GMV",
      "unit": "元",
      "type": "number",
      "target": 1200000,
      "baseline": 900000,
      "aggregation": "daily",
      "chart": "line",
      "compare": "target",
      "alert": { "rule": "below_target", "threshold": 0.8, "priority": "high" }
    },
    {
      "key": "ctr",
      "label": "点击率",
      "unit": "%",
      "type": "percent",
      "target": 3.4,
      "baseline": 2.1,
      "aggregation": "daily",
      "chart": "gauge",
      "compare": "baseline",
      "alert": { "rule": "mom_drop", "threshold": 0.2, "priority": "medium" }
    }
  ],
  "dimensions": ["daily", "channel"],
  "comparison": ["baseline", "target"]
}
```
   - `baseline`（基线）：MVP 阶段来源 = 预置 mock 历史基线（按「平台 + 目标类型」给默认值），真实接入后用历史数据计算。
2. **看板是"指标容器"**：读 schema → 按 `aggregation`/`chart`/`compare` 动态渲染指标卡片、图表、对比方式、预警规则。
3. **用户导入数据时**，把数据列映射到 schema 的指标 key（见 §6 字段映射）。

### 数据状态标注（防误导）
看板每个指标标注数据状态：`预估` / `实时(mock)` / `真实`，让观众清楚当前看到的是哪类数据。

### 降低配置成本：预置方案类型默认指标集
预置 3 类默认指标集，用户一键选用、可增删改：

| 方案类型 | 默认核心指标 |
|---|---|
| 拉新 | 新增会员数、拉新转化率、拉新权益领取量、CPA |
| 促活 | 召回率、复购率、券核销率、DAU 提升 |
| 去库存 | 滞销 SKU 动销、清仓 GMV、库存周转、券核销率 |

### 效果示例
同一个看板框架，不同方案渲染不同内容：拉新方案 → 显示「新增会员/拉新转化率/钩子领取量」；去库存方案 → 显示「滞销动销/清仓 GMV/库存周转」。

→ **看板长什么样，由方案的指标 Schema 决定，而非固定模板。**

---

## 8. 技术方案

### 架构（分层）
```text
前端(静态 HTML/CSS/JS, GitHub Pages)
  ├── 确定性逻辑层（前端 JS，零依赖保证演示稳定）
  │     ├── 规则引擎：券合规校验 / 漏洞扫描（防资损）
  │     ├── 预估模型：GMV = 曝光×点击率×转化率×客单价（MVP 简化版）
  │     └── 异常检测：阈值 / 环比 / 连续异常
  ├── 动态看板引擎：读 metricSchema → 渲染指标卡片/图表/对比/预警
  ├── 数据接入层：CSV/Excel 解析 + 字段映射(规则优先) + mock 数据流引擎
  └── LLM 层（Claude API，结构化输出 JSON Schema）
        ├── 方案生成   ├── 字段映射兜底   ├── 复盘报告 + 行动建议   └── 对话助手(tool use)
```

### 规则引擎：券合规校验（防资损）
内置确定性校验，LLM 生成 + 规则引擎把关（非 LLM 瞎编）：
1. 满减阈值 ≤ 商品毛利（防亏本）
2. 多券叠加冲突检测 + 叠加后成本上限
3. 活动总补贴成本 ≤ 预算
4. 选品库存 ≥ 预估销量（防超卖）
5. 规则漏洞扫描（如"满100减50 + 第二件半价"的漏洞）

### 技术含金量点
| 技术 | 价值 |
|---|---|
| 结构化输出（JSON Schema） | 方案 / 指标清单是结构不是散文，可落地 |
| 规则引擎（券校验） | 券规则"有边界"，防资损，非 LLM 瞎编 |
| 动态看板引擎（Schema 驱动） | 解决"不同方案不同数据"的核心难题 |
| 字段映射（规则优先 + LLM 兜底） | 列名不统一时的高可靠自动识别 |
| mock 数据流引擎（可替换适配器） | 实时感做实，架构预留真实 API/WebSocket |
| 异常检测 | 从"人盯数据"到"AI 报异常" |
| 对话 Agent（tool use） | 延续 prompt-architect 的能力线 |

### 关键设计声明（对齐承诺与实现）
- **预估模型**：MVP 为简化版乘法模型；真实系统预留「历史基线 + 渠道分解 + 大促因子」。文档与演示均明确这是简化版，不用"企业级"包装。
- **实时性**：V1 由 mock 数据流引擎模拟（体验真实、数据为模拟），真实实时性由 API/WebSocket 适配器预留接入，**不虚假宣称"真实时"**。
- **安全**：LLM API key 放 serverless 环境变量，前端永不暴露；真实数据接入需脱敏 + 权限隔离。
- **mock 数据流节奏**：刷新频率可配置，演示时用适中节奏，保证"看得清、有动感"。

### 务实落地策略
- **MVP 先纯前端**：规则引擎、预估模型、异常检测、动态看板、mock 数据流全是前端 JS，**不依赖后端不依赖 LLM 也能完整演示**。
- **LLM 走「真调用 + mock 降级」**：预留 serverless 后端（Vercel/Netlify Functions），无 key/断网自动切预置示例。
- 三层设计成立：**确定性算法保证稳定、LLM 保证 AI 含量、mock 保证演示零风险**。

---

## 9. 预警与复盘（体验优化）

### 预警：少而准，宁可漏报不可误报
- 规则：阈值（低于目标 X%）/ 环比（下滑 > Y%）/ 连续异常（连续 N 天偏离）。
- 三条体验约束：
  1. **优先级分级**（高/中/低），高优先级置顶。
  2. **同类聚合去重**（同指标连续异常合并为一条，避免刷屏）。
  3. **每条附可解释理由**（"CTR 环比下滑 22%，低于基线 2.1%，疑因资源位曝光稀释"）。

### 复盘报告：有增量价值，非数据复述
每条结论须满足：**因果推断 + 历史对比 + 一个可执行动作**。
- ❌ 反面："GMV 达标 80%。"
- ✅ 正面："GMV 低于目标 20%，主因 C 渠道转化未起量（历史基线 2.1%→本期 1.6%）；建议将 C 渠道资源位换到 B 渠道并下调满减门槛。"

---

## 10. MVP 范围与开发计划

**MVP 做**：新建项目（三要素 + 预设）→ 方案生成（含券校验 + 预估 + 指标 Schema）→ 数据接入（文件导入 + 字段映射 + mock 实时数据流）→ 动态看板（含冷启动预填充 + 数据状态标注）→ 异常预警（少而准）→ AI 复盘 + 对话助手。

**MVP 暂缓**：真实 API/WebSocket 数据源、真 RAG、多平台深度模板、定时任务、数据脱敏与权限体系。

| 步骤 | 产出 | 耗时 |
|---|---|---|
| 1 | 骨架：目录 + 项目列表 + 新建项目表单（含预设）+ 样式 | 1 天 |
| 2 | 策划模块：方案生成结构 + 券规则引擎 + 预估模型 + 指标 Schema | 2 天 |
| 3 | 数据接入：CSV 解析 + 字段映射（规则优先）+ mock 数据流引擎 | 1–2 天 |
| 4 | 动态看板：Schema 驱动渲染 + 冷启动预填充 + 异常检测 + 预警 | 2 天 |
| 5 | LLM 层：结构化输出 + 字段映射兜底 + 复盘 + 对话助手（mock 降级） | 2 天 |
| 6 | 部署 GitHub Pages + README + 动效打磨 | 1 天 |
| 7 | 录 demo 视频 + 求职叙事脚本 | 1 天 |

**节奏**：约 1 周出「策划 + 追踪」可演示版，2 周出完整闭环。

---

## 11. 附录：指标字典（示例）

| key | 中文 | 单位 | 类型 | 基线来源 | 预警示例 |
|---|---|---|---|---|---|
| gmv | GMV | 元 | number | 历史基线 | 低于目标 80% |
| ctr | 点击率 | % | percent | 历史基线 | 环比下滑 > 20% |
| cvr | 转化率 | % | percent | 历史基线 | 低于基线 |
| new_users | 新增会员 | 人 | number | 历史基线 | — |
| cpa | 拉新成本 | 元 | number | 目标上限 | 高于上限 |
| stock_turnover | 库存周转 | 次 | number | 安全线 | 低于安全线 |
| coupon_rate | 券核销率 | % | percent | 历史基线 | 低于预期 |
